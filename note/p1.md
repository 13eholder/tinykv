## standalone storage engine
Target:
1. 在badger库的KV接口上,实现封装;并为该封装层实现column family功能和storage接口

```go
type Storage interface {
    // Other stuffs
    Write(ctx *kvrpcpb.Context, batch []Modify) error
    Reader(ctx *kvrpcpb.Context) (StorageReader, error)
}
```

Hints:
1. 阅读kv/util/engine_util/{engine_util_test.go,doc.go}
2. 使用badger.Txn实现Storage.Reader函数,使用Discard函数结束badger.Txn

Todo:
1. 修改kv/storage/standalone_storage/standalone_storage.go,为其实现Storage接口


## raw api
封装standalone_storage即可